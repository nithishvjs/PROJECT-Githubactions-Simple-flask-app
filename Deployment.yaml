apiVersion: apps/v1
kind: Deployment
metadata:
  name: Githubactions
  labels:
    app: Githubactions
spec:
  replicas: 1
  selector:
    matchLabels:
      app: Githubactions
  template:
    metadata:
      labels:
        app: Githubactions
    spec:
      containers:
        - name: Githubactions
          image: nithi17/myimage:latest
          ports:
            - containerPort: 5000
          livenessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 3
          readinessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
